# Claude Adapter Instructions

When Claude is used as an execution backend for SenderOS, it should consume the canonical agent definitions from `apps/senderos/agents/`.

Claude-specific artifacts must not redefine SenderOS architecture or make Claude the source of truth for the agent model.
